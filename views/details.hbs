<section>
  {{#with room}}
  <h1>{{name}} Details</h1>
  <p>{{description}}</p>
  <p>Price per night: {{price}}€</p>
  <div>
    <img class="img" src="{{imgUrl}}" alt="room image">
  </div>
  <a href="/facility/{{_id}}/decorateRoom">Edit Faicilities</a>
  {{/with}}
</section>