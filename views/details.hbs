<section>
  {{#with room}}
  <h1>{{name}} Details</h1>
  <p>{{description}}</p>
  <p>Price per night: {{price}}€</p>
  <div>
    <img class="img" src="{{imgUrl}}" alt="room image">
  </div>
  <ul>
    {{#each facilities}}
    <li>{{label}} <img class="facility-icon" src="{{iconUrl}}" alt="icon"></li>
    {{/each}}
  </ul>
  <a href="/facility/{{_id}}/decorateRoom">Edit Facilities</a>
  {{/with}}
</section>